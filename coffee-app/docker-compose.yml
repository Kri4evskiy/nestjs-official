services:
  db:
    image:  postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
       POSTGRES_PASSWORD: pass123
    volumes:
      - postgres_data_coffee_app:/var/lib/postgresql

volumes:
  postgres_data_coffee_app:
